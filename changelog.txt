26.9.20
- removed content-script and <all_url> host permission, now it's purely no client-side javascript injection (even not isolated) which makes the web-extension safe, the existing ruleset is static and the service worker simply maintain the button states. the reason is that after testing, the chrome:// and edge:// pages that shows X was blocked by web-extension seems to not allow (unless you enable in the flags chrome pages as script's target with you should not do) any script injection. 

26.9.19
+ adding explicit all types of resourceTypes, which allows the blocking to be effective! browser often shows message blocked by web-extension. as the blocking is very effective now, I'm considering removing the content-script (still exists in this version).
+ adding readme and screenshots (video guide)
- reducing the amount of content-script runs to just once before page start.
- using the same `startup_handler` to handle `onInstalled` which is an event that runs when installed or reloaded, just one more "wakeup" to the service worker I guess..
- removing (commented-out code still exists) setting badge text and background color, I don't actually use it and it takes a lot of the promise timing as well as not being very effective, staying with icon and title and seems fine and more clean clear and simple). storing some of this (unused) information in the multi-language json files to save space in code.
* explicit fallback to api for chrome or browser.
* some icons cleanup, making the name 'internet disabled' 'internet enabled' to be more clear than 'ruleset disabled' (which means internet is enabled).

26.9.15
+ adding runtime.onStartup to trigger the service worker to wake up very early just to enable the ruleset seems to work fine now.

26.9.14
+ initial.
